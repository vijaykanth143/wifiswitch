import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const MapScreen = () => {
  return <Text style={{color: 'black'}}>HIidsfsdf</Text>;
};

export default MapScreen;

const styles = StyleSheet.create({});
